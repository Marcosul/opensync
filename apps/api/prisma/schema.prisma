generator client {
  provider      = "prisma-client-js"
  // bookworm-slim (Fly/Docker) usa OpenSSL 3; dev local continua com "native"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
